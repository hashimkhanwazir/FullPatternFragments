PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpr: <http://dbpedia.org/resource/>

SELECT  ?object ?name
WHERE
  { dbpr:Federal_government_of_the_United_States
              ?predicate  ?object .
    ?object   rdfs:label  ?name
  }
