PREFIX  rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX  dbpr: <http://dbpedia.org/resource/>

SELECT  ?object ?name
WHERE
  { dbpr:Communist_Party_of_Great_Britain
              ?predicate  ?object .
    ?object   rdfs:label  ?name
  }
