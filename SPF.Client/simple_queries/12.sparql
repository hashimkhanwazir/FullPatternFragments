# Star-shaped query: Retrieve event details happening in a city
SELECT ?eventName ?date WHERE {
    ?event <http://schema.org/location> <http://example.org/City2> .
    ?event <http://schema.org/name> ?eventName .
    ?event <http://schema.org/date> ?date .
}